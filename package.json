{
  "name": "kyle-belle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "http://kyle-belle.github.io",
  "workspaces": ["/packages/*"],
  "scripts": {
    "predeploy": "bun run --cwd packages/app predeploy",
    "deploy": "bun run --cwd packages/app deploy",
    "dev": "bun run --cwd packages/app dev",
    "build": "bun run --cwd packages/app build",
    "lint": "bun run --cwd packages/app lint",
    "preview": "bun run --cwd packages/app preview"
  }
}
